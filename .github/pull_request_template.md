## Change and motivation

## Validation

- [ ] Package inventory checker passes (manifest changes explicitly reviewed).
- [ ] CPU tests pass; no real model/scheduler execution in CI.
- [ ] No private values, outputs, credentials, site files or weights are included.
- [ ] Catalog and scientific/runtime changes, if any, are explicitly described.
- [ ] Validation claims distinguish fake-backend tests from real MD acceptance.

## Scientific/runtime impact

State "none" only if the protocol, QC, catalog and scientific execution remain unchanged.

